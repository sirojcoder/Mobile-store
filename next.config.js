/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
      domains: ["cdn.dummyjson.com"], // Faqat shu domen rasmlarini yuklashga ruxsat beramiz
   }
};

module.exports = nextConfig;
